<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.NetflixCloneTemplate" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <item name="colorPrimary">@color/inputColor</item>
        <item name="android:windowBackground">@color/corFundo</item>
        <item name="colorPrimaryDark">@color/corFundo</item>
        <!-- Customize your theme here. -->
    </style>
</resources>